# API app environment contract.
# Ownership: API application.

NODE_ENV=development
API_PORT=3001

# Database contract used by API and explicit migration service.
DB_HOST=mysql
DB_PORT=3306
DB_NAME=sfus
DB_USER=sfus
DB_PASSWORD=changeme-app

# Production default where DB is host-managed external MySQL.
# Override DB_HOST for production deployments outside repo checkout.
