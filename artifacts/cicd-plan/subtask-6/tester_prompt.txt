You are the Tester Agent.

Task summary:
- Implemented Subtask 6 CD thin shim: added manual GitHub Actions CD workflow entrypoint and delegated stage operations to shared `cicd/scripts/build-images.sh` + `cicd/config/image-matrix.yml`.
- Publish/deploy are explicitly gated off by default.

Modified files:
- .github/workflows/cd.yml
- cicd/scripts/build-images.sh
- cicd/config/image-matrix.yml

Acceptance criteria to validate (from Planner):
- `.github/workflows/cd.yml` is manually triggerable for any branch/ref.
- Workflow calls only `cicd/scripts` and `cicd/config` assets.
- Publish/deploy remain gated off by default.

Create test files in:
- cicd/tests/build-images.sh
- cicd/tests/run-validations.sh

Implementation context for testing:
- CD workflow now uses `workflow_dispatch` only with `git_ref`, `run_publish`, and `run_deploy` inputs.
- `build-images` job always runs and calls `bash cicd/scripts/build-images.sh cicd/config/image-matrix.yml build`.
- `publish` and `deploy` jobs are conditional (`if: inputs.run_publish/run_deploy == true`) and call the same shared script with `publish`/`deploy` operations.
- `build-images.sh` now supports operations: `build|validation` (normal behavior), `publish|deploy` (warning-only successful no-op gate), invalid operation (error).
- `image-matrix.yml` defaults now explicitly declare `publish_enabled: false` and `deploy_enabled: false`.

Suggested test command(s):
- bash cicd/tests/build-images.sh
- bash cicd/tests/run-validations.sh

Shared artifact directory:
- artifacts/cicd-plan/subtask-6
