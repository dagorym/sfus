## Documenter Report

### Files Updated
- **`cicd/docs/cicd.md`** — Clarified that `bash cicd/scripts/run-containers.sh` defaults to the documented `start` action so the exact repository-root run command matches the implemented scaffold behavior.
- **`cicd/tests/README.md`** — Replaced shorthand runner examples with exact repository-root validate/build/run commands and noted the container runner's default `start` action.

### Summary
Refined the existing Subtask 8 documentation so the canonical repository-root CI/CD commands are explicit in both docs while staying aligned with the implemented default config and container-runner behavior.

### Commit Message
`docs: clarify cicd developer commands`
