You are the Tester Agent.

Task: verify Milestone 1 Foundation Subtask 5 remediation cycle 2 for same-worktree smoke-validation repeatability.

Shared artifact directory: `artifacts/milestone-1-foundation/subtask-5`
Implementation commit to verify: `0f971c82358488eca2923b74ed4e901a9ef25f1c`

Modified files:
- `cicd/scripts/smoke-validate.sh`
- `cicd/docs/local-pipeline.md`

Acceptance criteria to validate:
- Scriptable smoke validation covers build, startup, migration execution, homepage reachability, and API health.
- The smoke flow is repeatable and safe for repeated or parallel execution from the same worktree.
- Existing CI/CD validation assets still support the Milestone 1 stack after the remediation.
- Deployment/operations documentation remains aligned with the implemented runtime behavior.

Remediation context:
- Prior remediation already removed fixed Compose `container_name` values and added overridable host ports.
- Remaining failures came from shared mutable worktree state (`.env`, `apps/web/.env`, `apps/api/.env`) and shared build/runtime interference during concurrent smoke runs.
- `cicd/scripts/smoke-validate.sh` now copies root/web/api env input into a per-run runtime area under `git rev-parse --git-path smoke-validate`, generates a per-run Compose file that points to those isolated env copies, and cleans them up on exit.
- The script also reserves host ports through locked reservation files so concurrent runs from the same worktree cannot choose the same ports.
- The smoke build/startup/migration flow remains otherwise unchanged.
- `cicd/docs/local-pipeline.md` now documents the isolated per-run smoke runtime behavior.

Existing validation commands already run by Implementer:
- `bash -n cicd/scripts/smoke-validate.sh`
- `bash cicd/scripts/smoke-validate.sh`
- `bash cicd/scripts/smoke-validate.sh` in two concurrent shells
- `bash cicd/tests/run-validations.sh`
- `bash cicd/tests/run-validations.sh && bash cicd/scripts/smoke-validate.sh`

Suggested test locations / conventions:
- CI/CD contract tests: `cicd/tests/run-validations.sh`
- Smoke runtime script under test: `cicd/scripts/smoke-validate.sh`

Please verify especially that concurrent smoke runs from this same worktree no longer delete each other's env files or collide on reserved ports, and reuse this shared artifact directory for tester outputs passed forward to the verifier.
