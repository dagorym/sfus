Your role is 'tester'. Your task is as follows:

Task summary:
- Build the admin forums management page at /admin/forums exposing full categories + boards CRUD, consuming the forums-admin-client from CO8.

Modified files:
- apps/web/app/admin/forums/page.tsx

Acceptance criteria to validate (from Planner):
- AC1: /admin/forums gated like other admin pages — resolveProtectedSession redirect, non-admin denied, admin sees page
- AC2: Lists all categories with boards via adminListCategories
- AC3: Category CRUD — create/edit/delete/reorder; no-boards delete surfaced as friendly message
- AC4: Board CRUD — create/edit/delete/reorder with full field set including scopeType and visibility
- AC5: No dangerouslySetInnerHTML; user text as React nodes; encodeURIComponent on URL segments
- AC6: Errors surfaced via standard envelope as friendly messages

Create test files in:
- apps/web/app/admin/forums/forums-admin.spec.ts — source-audit pattern matching forums-admin-client.spec.ts and public-shell.spec.ts

Implementation context for testing:
- New page only — no existing files modified
- Page uses "use client", resolveProtectedSession("/admin/forums"), and hasGlobalRole(session.user, "admin") matching all other admin pages
- Reuses apps/web/app/auth-shell.module.css (no new CSS file needed)
- No dangerouslySetInnerHTML anywhere; all user-supplied text rendered as React text nodes
- encodeURIComponent on URL segments is handled by the forums-admin-client module (page does not construct dynamic URL segments directly)
- Category delete: proactively checks boards.length>0 client-side and also catches 400 API responses with a /board|must be empty|not empty/i regex for a friendly message
- Reorder: uses adminReorderCategories / adminReorderBoards with splice-based id ordering
- Security surfaces: admin auth gate (resolveProtectedSession + hasGlobalRole admin), all API calls via forums-admin-client with credentials:include, no XSS vectors

Suggested test command(s):
- pnpm --filter web exec tsc --noEmit
- pnpm --filter web run lint
- pnpm vitest run --root apps/web
- pnpm --filter web run build

Existing validations expected to fail only because approved behavior changed:
- components/authoring-components.spec.ts and components/user-avatar.spec.ts: pre-existing worktree react-resolution failures, not regressions from CO9

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/milestone-4-forums-closeout/CO9

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
