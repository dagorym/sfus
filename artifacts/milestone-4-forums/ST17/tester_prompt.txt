Your role is 'tester'. Your task is as follows:

## Context

This is the TESTER-DRIVEN REMEDIATION pass for ST17. The prior Tester run returned FAIL on a single
defect: `pnpm lint` failed (exit 1) because `apps/web/components/user-avatar.tsx` (~line 102)
rendered a native `<img>` element without an eslint-disable comment, tripping the
`@next/next/no-img-element` rule under `--max-warnings=0`. Everything else was GREEN.

The implementer has applied the one allowed fix: added `// eslint-disable-next-line @next/next/no-img-element`
on the line immediately above the `<img>` in user-avatar.tsx, matching the established repo convention
used in `apps/web/app/[slug]/page.tsx:118-119` and `apps/web/app/pages/[slug]/page.tsx:67-68`.

## Modified files

- `apps/web/components/user-avatar.tsx`

## Acceptance criteria to validate (from Planner)

1. `pnpm lint` exits 0, 0 warnings — THIS IS THE SPECIFIC FIX UNDER REPAIR. Confirm explicitly with pasted output.
2. 407 web tests pass, 0 fail (28 new ST17 specs — `components/user-avatar.spec.ts` 16 tests, `app/users/users-profile.spec.ts` 12 tests — must still execute and pass, non-vacuous).
3. `pnpm typecheck` exits 0, 0 errors.
4. No ST14 five-field allowlist regression.
5. No ST16 regression.

## Validation commands

Run from the worktree root:

1. `pnpm --dir <worktree> install --frozen-lockfile`
2. `<worktree>/node_modules/.bin/vitest run --root apps/web` — expect 407 pass, 0 fail
3. `pnpm --dir <worktree> typecheck` — expect 0 errors
4. `pnpm --dir <worktree> lint` — expect exit 0, 0 warnings (THE FIX BEING VERIFIED)

## Implementation context

The fix is a one-line comment insertion: `// eslint-disable-next-line @next/next/no-img-element`
added immediately above the `<img>` element in `apps/web/components/user-avatar.tsx`. The avatar
component uses a dynamic user-supplied image from the gated `/api/media/<id>` path with an
`onError`->initials fallback; raw `<img>` is the repo-consistent choice for this use case.
No behavior change — purely suppresses the lint warning using the same pattern as the rest of the codebase.

Prior tester FAIL artifacts preserved in `artifacts/milestone-4-forums/ST17/history/tester-1-fail/`.

## Test file locations

- `apps/web/components/user-avatar.spec.ts` (16 tests — ST17 new)
- `apps/web/app/users/users-profile.spec.ts` (12 tests — ST17 new)

## Existing validations expected to fail only because approved behavior changed

None.

## Shared artifact directory

`artifacts/milestone-4-forums/ST17`

## Startup behavior

If acceptance criteria and implementation context are present, begin testing work immediately
and continue in the same run. Do not stop after activation or role acknowledgment.

## Completion gate

Do not report success unless all required artifacts exist and all changes are committed.
