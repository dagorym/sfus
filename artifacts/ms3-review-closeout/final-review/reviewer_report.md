Reviewer Report

Feature plan reviewed:
- plans/ms3-review-closeout-plan.md (Milestone 3 Review Closeout: 8 subtasks closing every WARNING, NOTE, missed-functionality item, and follow-up request from artifacts/ms3-landing-refresh-and-review-followups/final-review/reviewer_report.md, plus the user-reported stale Milestone 2 shell copy; Workflow Action WA1 retroactive security reviews completed in-cycle)

Inputs reviewed:
- artifacts/ms3-review-closeout/subtask-{1..8}/ implementer/tester/documenter/verifier reports and result JSON (all chains complete; superseded passes archived under each history/ subdirectory)
- Specialist security artifacts for subtasks 5, 7, 8 (security_report.md / security_result.json): subtask-5 pass-2 PASS (0/0/3), subtask-7 PASS (0/0/5), subtask-8 PASS (0/0/5)
- Verifier verdicts: subtask-1 PASS (0/0/0); subtask-2 PASS (0/0/2); subtask-3 pass-2 PASS (0/0/1, pass-1 WARNING was a diff-base artifact cleared against merge-base 141eea3); subtask-4 pass-2 PASS (0/0/0); subtask-5 pass-2 PASS (0/0/1); subtask-6 pass-2 PASS (0/0/1, pass-1 import.meta fix broke the tsc CJS build and was remediated with __dirname); subtask-7 PASS (0/0/3); subtask-8 pass-2 PASS (0/0/3, stale Swagger decorators fixed)
- WA1 retroactive security artifacts for the PRIOR plan: artifacts/ms3-landing-refresh-and-review-followups/subtask-3/security_report.md+security_result.json (commit ef9c964, CONDITIONAL PASS) and subtask-4/security_report.md+security_result.json (commit f54c02a, CONDITIONAL PASS) — both exist and both code-change WARNINGs are confirmed closed by subtasks 7 and 8
- Prior final review artifacts/ms3-landing-refresh-and-review-followups/final-review/reviewer_report.md (0 blocking, 5 warnings, 6 notes, 4 follow-ups) cross-checked finding-by-finding against the plan's Reviewer-Finding Coverage Map
- Direct code inspection on the merged ms3-claude tree (92147ae): apps/web/app/layout.tsx, apps/web/app/public-shell.spec.ts, apps/web/app/pages/pages-client.ts, apps/web/app/blog/blog-client.ts, apps/web/app/[slug]/page.tsx, apps/api/src/pages/pages.service.ts, apps/api/src/pages/pages.service.integration.test.ts, apps/api/src/navigation/navigation.service.ts, apps/api/src/navigation/navigation.controller.test.ts, apps/api/src/navigation/navigation.service.test.ts, apps/api/src/blog/blog.controller.ts, apps/api/src/blog/blog.service.ts, apps/api/src/blog/blog.service.test.ts, apps/api/src/media/media.controller.test.ts, apps/api/package.json, cicd/config/validation-config.yml
- Documentation: docs/README.md (reserved-slug list, transactional create guarantee, error-envelope adoption, authenticated-nav filtering guarantee, comment-creation 404 contract), docs/website-launch-guide.md (integration-test invocation; no stale 6-failure caveat anywhere in docs/ or cicd/docs/), cicd/docs/cicd.md + cicd/docs/local-pipeline.md (pages-service-integration entry), docs/deferred-tasks.md (TOCTOU line 16 + three WA1 security notes lines 17-19)
- Validation runs by this reviewer: API suite via the documented --filter invocation 297 passed / 2 skipped (integration spec skips cleanly with the documented message, no DB required); web suite 245/245; API tsc build clean; workspace lint and typecheck clean; DB-gated integration spec independently run against the live dev MySQL (host port 43306) with SFUS_DB_INTEGRATION=1 — 2/2 PASS (real-FK round-trip + rollback proof)

Overall feature completeness:
- Complete. All eight subtasks are delivered, tested, documented, verified, and (where required) security-reviewed, and every feature-level acceptance criterion was independently re-verified on the merged tree.
- Shell copy (AC1): layout.tsx carries the exact D1 Milestone 3 strings (metadata line 14, eyebrow line 29, footer line 46); the only 'Milestone 2' occurrences in apps/web are the negative assertions in public-shell.spec.ts that pin its absence.
- Error envelope (AC2): all 8 pages-client reads and all 16 blog-client reads resolve payload?.error?.message first; no payload?.message-only read remains; the tester-added regression specs pin the chains (pages.spec.ts and blog.spec.ts both green); docs/README.md lines 242/394 describe the adopted pattern.
- Transactional create + schema-enforced proof (AC3): create() wraps all three writes in pageRepository.manager.transaction (pages.service.ts:146-182) with the guarantee stated in the JSDoc (lines 120-131); the env-gated integration spec exists, skips cleanly by default, and passed 2/2 for this reviewer against the live dev MySQL with the real fk_page_revisions_page_id constraint; test:integration script and the pages-service-integration cicd entry are in place and documented.
- Reserved 'pages' slug (AC4): present in API RESERVED_PAGE_SLUGS (pages.service.ts:22) and mirrored in the web RESERVED_SLUGS ([slug]/page.tsx:40) — eleven entries each, parity pinned by tests; bare-/pages static-route behavior documented (docs/README.md line 414); fail-closed confirmed by the pass-2 security review.
- Suites green (AC5): API 297 passed / 2 skipped under the documented --filter invocation (cwd defect fixed via __dirname; no process.cwd() source-path resolutions remain in API tests); web 245/245 including updated shell-copy assertions, the NOTE 2 login error-copy source contracts (public-shell.spec.ts:170-174), and the admin error-chain specs; serveImage happy-path test present (media.controller.test.ts:309); API tsc build green (the subtask-6 pass-1 TS1470 regression was caught and remediated).
- WA1 closure (AC6): both retroactive security artifact pairs exist for the prior plan's subtasks 3 and 4 (ef9c964, f54c02a); the prior report's WARNING 4 is closed; every code-change finding from them is closed by subtasks 7 and 8 and confirmed closed by the in-cycle security stages.
- Authenticated-nav filtering (AC7): findForAuthenticatedUser applies filterByLinkedTargetVisibility/isLinkedTargetPubliclyVisible for non-admin callers and preserves the admin bypass (navigation.service.ts:117-145); 16 new tests including predicate-pinning (status: 'published' asserted 10 times in the nav suite); security stage PASS with both WA1 warnings confirmed closed; documented in docs/README.md line 416.
- Comment-creation oracle (AC8): resolvePostId falls back to findPublishedById (blog.controller.ts:354-363) and the createComment non-public guard throws NotFoundException('Blog post not found.') (blog.service.ts:386) while the locked-public-post 403 is preserved (blog.service.ts:391); oracle-parity tests assert identical class+message across nonexistent/draft/unpublished/future-scheduled; LessThanOrEqual operator-type pinned in blog tests; Swagger decorators corrected in pass-2; docs/README.md lines 200/256/272 state the 404 contract.
- Registers and docs (AC9): docs/deferred-tasks.md lines 16-19 record the TOCTOU item and all three D9 security notes; cross-subtask README edits from five documenter stages are mutually consistent (eleven-slug count, envelope description, transaction guarantee, nav filtering, 404 contract all coherent).
- Cross-subtask integration: no conflicts between the five README-editing chains; the API/web reserved-slug mirror is parity-tested; the subtask-6 build regression demonstrates the remediation loop worked (the API build is now part of the validated gate). No feature-level security gap remains: all three security-marked subtasks received specialist stages with final PASS, and the remaining security observations are NOTE-level and recorded.

Findings

BLOCKING
- None

WARNING
- None

NOTE
- apps/api/src/navigation/navigation.service.test.ts - The navigation publication predicate pin asserts the publishedAt key/shape but not the LessThanOrEqual FindOperator type, unlike the blog suite which pins the operator type explicitly (blog.service.test.ts:204-238).
  Subtask-7 security NOTE 1 (accepted residual): a hypothetical regression replacing LessThanOrEqual with plain equality on the nav path would still pass the nav suite, though the shared findPublishedById predicate is operator-pinned on the blog side. Low risk; an easy symmetry improvement at the next touch of the nav tests.
- apps/api/src/pages/pages.service.ts:353 - Reserved-slug enforcement is prospective-only: assertSlugValid runs on create and slug-changing update, so a hypothetical pre-existing standalone page with slug 'pages' (legal before this plan) would remain operable and publishable; no legacy-data sweep was performed.
  Carried forward from the subtask-5 security/verifier NOTEs and materially mitigated: the web-side reserved branch now prevents such a row from ever being served at /pages, so the residual is an inconsistent admin state, not a leak. A one-time check of deployed databases for a 'pages'-slug row would fully close it (plan Risk #4 assumption: no such row exists).
- apps/web/app/[slug]/page.tsx:56 - An admin-authored bare-/pages navigation item is now always rendered by the API as a static route but resolves web-side to a deliberate not-found state (a dead link), because no /pages index route exists.
  Subtask-5 security NOTE 2 (accepted): fail-closed, affects only nav-admin-authored links, and becomes consistent automatically if a /pages index page is ever added. Documented behavior; no leakage.
- apps/api/src/pages/pages.service.integration.test.ts - The rollback proof (Test 2) drives a constraint violation inside a test-local transaction rather than forcing service.create() itself to fail mid-transaction; the spec documents why direct failure-routing through create() is structurally infeasible without production changes.
  Accepted scope-of-proof from the subtask-4 verifier remediation: Test 1 proves the real create() round-trip against the enforced FK, and Test 2 proves MySQL rollback semantics for the same write shape. The combination covers the regression class that shipped the original FK bug; the residual gap (create()'s own error routing under a real DB) is small and explicitly documented rather than hidden.
- apps/api/src/blog/blog.service.ts:398 - Pre-existing second-order existence distinguishers remain on the comment-creation path after the visibility gate: distinct 400 messages for parentId ('Parent comment not found.' vs 'does not belong to this post') and for imageId scope let an authenticated member confirm an arbitrary comment/media UUID exists somewhere.
  Out of this plan's scope and not worsened by it (subtask-8 security NOTE 4); exploitability is very low (random UUIDs, 400-level, no content disclosure). The security report nominates it for the deferred data-minimization bucket, but docs/deferred-tasks.md is planner-owned and does not yet record it — converted to a follow-up request below so it is not lost.
- apps/api/src/blog/blog.controller.test.ts:230 - The createComment Swagger-decorator source-contract assertions use a 600-character lookback window from the method signature; the current decorator block (~300 chars) leaves headroom, but substantial decorator growth would require updating the constant.
  Informational fragility of the text-extraction source-contract style (subtask-8 pass-2 verifier NOTE); an expected decorator could move out of the window only after large edits to that block, and the targeted assertions would then fail loudly rather than silently pass.

Missed functionality or edge cases:
- None. Every prior-review WARNING (1-5), NOTE (1-6), missed-functionality item (1-3), and follow-up request (1-4) is demonstrably closed or explicitly accounted for exactly as the plan's Reviewer-Finding Coverage Map states: WARNINGs 1-2 by subtask-2 (verified: zero payload?.message-only reads remain), WARNING 3 + NOTE 6 by subtask-3 (verified transaction + JSDoc), WARNING 4 by WA1 (artifacts exist: ef9c964, f54c02a), WARNING 5 by subtask-4 (integration spec independently re-run green against live MySQL), NOTE 1 by subtask-6 (297 passed / 2 skipped via --filter), NOTE 2 by subtask-1's tester stage (login error-copy contracts verified in public-shell.spec.ts), NOTE 3 by subtask-5, NOTE 4 by the D5 register entry (deferred-tasks line 16), NOTE 5 by subtask-6's tester stage (serveImage happy-path test verified).
- The WA1 security findings are closed: the authenticated-nav metadata leak by subtask-7 and the comment-creation existence oracle by subtask-8, each with its own specialist security PASS confirming closure; the three informational WA1 notes are recorded in docs/deferred-tasks.md lines 17-19 (D9), confirmed rather than re-litigated.
- Remaining observations are the six NOTE-level residuals above — all accepted characteristics, documented scope-of-proof limits, or pre-existing out-of-scope items; none represents unshipped plan scope.

Follow-up feature requests for planning:
- At the next planning cycle, append the comment-creation second-order existence distinguishers to docs/deferred-tasks.md alongside the existing data-minimization entry (line 17): after the visibility gate, distinct 400 messages for parentId ('Parent comment not found.' vs 'Parent comment does not belong to this post.', blog.service.ts:398-403) and for imageId scope (blog.service.ts:413-419) let an authenticated member confirm foreign comment/media UUIDs exist; normalizing these messages (or folding them into the payload-trimming work) removes the last known authenticated existence signal on the comment path. Nominated by the subtask-8 security review (NOTE 4); very low severity, planner-owned register edit plus a small API change when scheduled.
- Optional low-priority hardening, suitable to batch with the next navigation or pages touch: (a) pin the LessThanOrEqual FindOperator type in navigation.service.test.ts's publication-predicate assertions to match the blog-side operator-type pinning, closing the shape-only residual from subtask-7 security NOTE 1; (b) perform a one-time check of deployed environments for a legacy standalone_pages row with slug 'pages' (plan Risk #4 assumed none exists) and delete or rename it if found, fully closing the prospective-only reserved-slug enforcement NOTE.

Artifacts written:
- artifacts/ms3-review-closeout/final-review/reviewer_report.md
- artifacts/ms3-review-closeout/final-review/reviewer_result.json

Final outcome:
- PASS
