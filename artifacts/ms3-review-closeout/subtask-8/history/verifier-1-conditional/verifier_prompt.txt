Your role is 'verifier'. Your task is as follows:

Task summary:
- ms3-review-closeout subtask-8: closed the authenticated existence oracle on POST /api/blog/:postIdOrSlug/comments. Non-public posts (draft, unpublished, future-scheduled) are now indistinguishable from nonexistent ones on the comment-creation path. resolvePostId in blog.controller.ts uses findPublishedById instead of findById for the UUID fallback so a non-public post by UUID is treated identically to a nonexistent one at the controller layer. createComment in blog.service.ts non-public guard changed from ForbiddenException to NotFoundException (defense-in-depth). The commentsLocked ForbiddenException is intentionally preserved.

Acceptance criteria to verify:
- POST /api/blog/:postIdOrSlug/comments returns indistinguishable 404 for nonexistent, draft, unpublished, and future-scheduled posts -- all produce identical NotFoundException with message 'Blog post not found.'
- Comment creation on genuinely public posts is unchanged; a locked public post still returns 403 with message 'Comments are locked on this post.'
- All other comment routes (list, moderation, lock/unlock) are behaviorally unchanged.
- Security: no authenticated member can distinguish an existing non-public post from a nonexistent one via the comment-creation route (oracle-parity suite passes).
- API builds, typechecks, and blog unit suite passes (83 tests).
- docs/README.md accurately reflects 404 (not 403) for non-public posts on the comment-creation route, with 403 preserved only for commentsLocked=true on a public post.

Implementation branch or worktree context:
- Documenter branch: ms3-claude-subtask-8-documenter-20260606 (worktree: /home/tstephen/repos/worktrees/ms3-claude-subtask-8-documenter-20260606)
- Tester branch: ms3-claude-subtask-8-tester-20260606
- Implementer branch: ms3-claude-subtask-8-implementer-20260606
- Base for diff: ms3-claude

Files modified by Implementer, Tester, and Documenter to inspect:
- apps/api/src/blog/blog.controller.ts (Implementer: resolvePostId now uses findPublishedById for UUID fallback, ~line 354; JSDoc updated to reflect published-only predicate on both branches)
- apps/api/src/blog/blog.service.ts (Implementer: createComment non-public guard changed to NotFoundException, ~line 382; inline comment added explaining defense-in-depth)
- apps/api/src/blog/blog.service.test.ts (Tester: 3 assertions updated, predicate assertions tightened, oracle-parity suite added, locked-post regression added)
- apps/api/src/blog/blog.controller.test.ts (Tester: new file pinning resolvePostId fallback wiring)
- docs/README.md (Documenter: three stale 403 statements updated to 404 for non-public posts on comment-creation path; commit 3ba4cd6)

Commands executed:
- pnpm install --frozen-lockfile (worktree context)
- pnpm --filter @sfus/api exec vitest run src/blog/ -- 83 tests pass
- pnpm --filter @sfus/api run build -- exits 0
- pnpm typecheck -- exits 0
- pnpm lint -- 1 pre-existing navigation.controller.test.ts error (unrelated)

Final test outcomes:
- 83 blog tests pass (79 service + 4 controller); 0 failures introduced by this subtask.
- Oracle-parity suite confirms all 4 non-public post variants (nonexistent/draft/unpublished/future-scheduled) throw NotFoundException with identical message.
- commentsLocked ForbiddenException regression passes -- guard is preserved for locked public posts.
- resolvePostId controller wiring pinned: slug branch resolves without UUID fallback; UUID fallback called when slug misses; both branches apply published-only predicate.
- LessThanOrEqual predicate assertions tightened for findPublishedBySlug and findPublishedById.

Documentation changes to verify:
- docs/README.md line 199: future-scheduled comment response updated from 403 to 404 with oracle disclosure note
- docs/README.md line 253: POST /api/blog/:postId/comments response code table updated -- 404 for non-public/nonexistent, 403 only for commentsLocked=true on a public post
- docs/README.md line 269: Comment Authorization Model paragraph updated -- 404 for draft/unpublished/future-scheduled, 403 only for commentsLocked on public post

Plan and diff context:
- Plan: plans/ms3-review-closeout-plan.md, subtask-8
- Base branch for diff: ms3-claude

Shared artifact directory:
- artifacts/ms3-review-closeout/subtask-8

Startup behavior:
- If the documenter branch/worktree context and enough story context are present, begin verification immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, or diff review when verification work can proceed.

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
- Verify implementation, tests, and updated documentation together as a single review surface.
