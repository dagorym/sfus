Your role is 'tester'. Your task is as follows:

Task summary:
- ST-5 remediation pass: add DoS size guard to DocsService.getDiff — expose DOCS_DIFF_MAX_BODY_BYTES and DOCS_DIFF_MAX_LINES as named constants in docs.types.ts; reject with BadRequestException(400) before entering the O(m*n) LCS DP table when either revision body exceeds the cap.

Modified files:
- apps/api/src/docs/docs.types.ts
- apps/api/src/docs/docs.service.ts

Acceptance criteria to validate (from Planner):
- DOCS_DIFF_MAX_BODY_BYTES and DOCS_DIFF_MAX_LINES are exposed as named constants in docs.types.ts (discoverable and documentable).
- DocsService.getDiff checks Buffer.byteLength of each revision body against DOCS_DIFF_MAX_BODY_BYTES before splitting into lines.
- DocsService.getDiff checks line count of each split body against DOCS_DIFF_MAX_LINES before entering the O(m*n) LCS DP table.
- Oversized bodies (bytes or lines) are rejected with BadRequestException(400) and a clear message naming the violated limit.
- At-or-below-cap revision bodies produce the same deterministic diff output as before — no behavior change for normal inputs.
- No new environment variable added (environment.ts is out of scope).
- All existing validations remain green.

Create test files in:
- apps/api/src/docs/docs.service.test.ts — add unit tests covering the new BadRequestException paths (over-cap body bytes, over-cap line count, at-cap body passes).
- apps/api/src/docs/docs.controller.test.ts — optionally add a controller-layer test confirming 400 is surfaced when the service throws BadRequestException from the size guard.
- apps/api/src/docs/docs.service.integration.test.ts — optionally add a mid-rollback transaction-failure proof (forcing duplicate revision_number) to make the P10 invariant explicit for rollback (pre-existing gap, non-blocking per verifier NOTE).

Implementation context for testing:
- docs.types.ts: Added DOCS_DIFF_MAX_BODY_BYTES = 512_000 (512 KB per revision body) and DOCS_DIFF_MAX_LINES = 5_000 (lines per revision body) as exported named constants in a new 'Diff size limits' section. Full JSDoc explains rationale and worst-case DP table bound (5 000 * 5 000 = 25 000 000 cells).
- docs.service.ts: Added value import of DOCS_DIFF_MAX_BODY_BYTES and DOCS_DIFF_MAX_LINES alongside the existing 'import type' block.
- docs.service.ts getDiff: After fetching both revisions and before body.split('\n'), added (1) byte-length check: Buffer.byteLength(body, 'utf8') > DOCS_DIFF_MAX_BODY_BYTES -> throw BadRequestException with message 'Revision body exceeds the maximum allowed size for diff (NNN bytes).'; (2) line-count check: lines.length > DOCS_DIFF_MAX_LINES -> throw BadRequestException with message 'Revision body exceeds the maximum allowed line count for diff (NNN lines).'. Both checks fire before any O(m*n) work begins.
- The guard is deterministic and adds negligible overhead for normal-sized inputs. The LCS algorithm and diff output are completely unchanged for inputs below the cap.
- The fix closes the unauthenticated DoS surface flagged as WARNING in the security_report.md and verifier_report.md from the verifier-1-warn pass.
- The Documenter must document DOCS_DIFF_MAX_BODY_BYTES (512 KB) and DOCS_DIFF_MAX_LINES (5 000 lines) in docs/features/documents.md in the diff endpoint section (previously noted as a gap in security_report.md NOTE and verifier_report.md NOTE).

Suggested test command(s):
- pnpm -r --filter ./apps/api run typecheck
- pnpm -r --filter ./apps/api run lint
- pnpm -r --filter ./apps/api run test
- pnpm -r --filter ./apps/api run build

Existing validations expected to fail only because approved behavior changed:
- None

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/ms5-documents-wiki/ST-5

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
