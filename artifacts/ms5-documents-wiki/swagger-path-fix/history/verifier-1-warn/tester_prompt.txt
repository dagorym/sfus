Your role is 'tester'. Your task is as follows:

Task summary:
- Move Swagger UI mount from /api/docs to /api/swagger to fix route collision with Documents API index route.

Modified files:
- apps/api/src/index.ts

Acceptance criteria to validate (from Planner):
- Swagger UI serves at /api/swagger (not /api/docs)
- OpenAPI JSON document serves at /api/swagger/openapi.json (not /api/docs/openapi.json)
- GET /api/docs no longer returns Swagger HTML — the Documents API index route is no longer shadowed
- Only apps/api/src/index.ts was modified

Create test files in:
- apps/api/src/index.test.ts — update the swagger-path assertion on ~line 169-174 to expect 'api/swagger' and 'api/swagger/openapi.json' instead of 'api/docs' and 'api/docs/openapi.json', and rename the test title accordingly
- apps/api/src/http.integration.test.ts — if the test boots a real Nest app, add a regression test asserting that GET /api/docs returns JSON (not Swagger HTML) when swaggerEnabled=true

Implementation context for testing:
- Root cause: SwaggerModule.setup was mounted at 'api/docs' which collided with the MS5 Documents API GET /api/docs index route.
- Fix: Changed SwaggerModule.setup path from 'api/docs' to 'api/swagger' and jsonDocumentUrl from 'api/docs/openapi.json' to 'api/swagger/openapi.json' in apps/api/src/index.ts lines 87-89.
- Also updated the inline comment at line 58 which referenced '/api/docs' as the Swagger mount point.
- No other files were changed. The Documents API routes are untouched.
- The change is gated by environment.swaggerEnabled (true in dev, typically false in production).
- GET /api/docs/recent and other sub-paths were NOT affected by the collision — only the exact /api/docs index was shadowed by Swagger HTML.

Suggested test command(s):
- pnpm --filter=api lint
- pnpm --filter=api typecheck
- pnpm --filter=api test
- pnpm --filter=api build

Existing validations expected to fail only because approved behavior changed:
- apps/api/src/index.test.ts: test 'boots the API with the /api prefix and serves Swagger at /api/docs when enabled' (~line 158) — asserts SwaggerModule.setup was called with 'api/docs'. After this change it asserts the wrong (old) path. This is an APPROVED behavior change. The Tester must update the assertion to expect 'api/swagger' and 'api/swagger/openapi.json'.

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/ms5-documents-wiki/swagger-path-fix

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
