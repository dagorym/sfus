# Deferred Tasks

_Register status (2026-06-07, post `deferred-cleanup`): the `plans/deferred-cleanup-plan.md` cycle completed with a CONDITIONAL PASS final review (`artifacts/deferred-cleanup/reviewer_report.md`); all 21 entries it scheduled were verified delivered against the actual code and removed from this register. Two surviving entries were updated to their current state, and four new entries were recorded from the final review's open findings. Markers: **Slated** = owned by the named future milestone per `star_frontiers_rpg_website_design.md` / `plans/sfus-implementation-plan.md`; **Design-doc feature** = in the design doc but not yet assigned to a milestone (suggested target noted); **Watch / Operational / Conditional** = tracked, but not code work now; **Fix on next … touch** = small repair to batch with the next matching dev cycle._

- **Design-doc feature (§4, §24) — no milestone assigned; suggested: a future identity follow-up pass.** Milestone 2: Add additional external authentication providers beyond Google and GitHub, including Facebook and other common identity sources, using the same provider-linking foundation.
- **Design-doc feature (§4) — no milestone assigned; suggested: a future identity follow-up pass.** Milestone 2: Add username changes after initial selection, including rate limiting, profile URL history or redirects, and audit-safe handling for mentions and identity continuity.
- **Slated: Milestone 6 (Downloads & Storage Pipeline; design §5.3).** Milestone 3: Add arbitrary file attachments and broader upload/download support beyond the shared image-only baseline for blog posts, standalone pages, and blog comments.
- **Design-doc feature (§5.7) — no milestone assigned; suggested: a future pages/content polish milestone.** Milestone 3: Add standalone-page block-builder capabilities such as hero, image, and CTA blocks beyond the shared editor plus revision-history scope.
- **Design-doc feature (§5.8) — conditional; revisit only if future site growth demands deeper hierarchy.** Milestone 3: Add navigation trees deeper than one level of dropdown children if future site growth proves the extra hierarchy is necessary.
- **Slated: Milestones 7–8 (Projects; design §5.6).** Milestone 3: Add project-scoped authoring-policy controls such as owner-only, moderator-only, or member-authoring selection when the Projects milestone introduces project content ownership and membership management.
- **Slated: Milestone 5 (Documents Wiki; design §5.5).** Milestone 3: Keep the full wiki/documents feature set, including nested wiki hierarchy and broader document workflows, deferred to Milestone 5.
- **Slated: Milestone 11 (SEO/social; design §23).** Milestone 3: Add SEO surfaces — OpenGraph/meta tags, sitemaps, and canonical URLs for blog posts and standalone pages (design §12; roadmap Phase 5).
- **Slated: Milestone 11 (Moderation; design §13).** Milestone 3: Add a reports / moderation queue for arbitrary objects beyond per-comment moderation (design §13; forums-era scope).
- **Design-doc feature (§9) — no milestone assigned; suggested: the next content milestone that reuses the shared editor.** Milestone 3: Add a per-user editor-mode preference (e.g. `user_settings.content_prefs`) plus a unified media-library picker with drag-and-drop, beyond the shared per-field image upload (design §10).
- **Slated: Milestone 11 (Accessibility; design §9).** Milestone 3: Complete a full WCAG 2.1 AA accessibility sweep beyond the keyboard-accessible navigation delivered in Milestone 3 (design §11.1; roadmap Phase 5).
- **Design-doc feature (§9, §18) — no milestone assigned; suggested: the next content milestone that reuses the shared editor.** Milestone 3: Add a true WYSIWYG editing surface; Milestone 3 ships Markdown authoring with live preview only.
- **Slated: Milestone 4 (forum anti-spam) / Milestone 11 (rate limits; design §13).** Milestone 3: Add comment rate-limiting / anti-spam controls (design rate-limits to Phase 5 and forum anti-spam to Milestone 4; the current stack has no Redis).
- **Conditional (performance) — stays deferred until the nav tree actually grows.** Milestone 3: Batch or cache the navigation publication-filter lookups if the nav tree grows — the public (and, post-closeout, authenticated) nav paths perform bounded per-item indexed point queries today (performance note from the retroactive MS3 security review, 2026-06).
- **Operational action (deployed environments, not repo code) — stays in the register until ops runs it.** Milestone 3: Perform a one-time check of deployed environments for a legacy `standalone_pages` row with slug `pages` (legal before the reserved-slug plan; plan Risk #4 assumed none exists) and delete or rename it if found, fully closing the prospective-only reserved-slug enforcement residual — `assertSlugValid` runs only on create and slug-changing update (MS3 closeout final review follow-up, 2026-06).
- **Watch entry (conditional).** Milestone 3: Strengthen the pages-service integration rollback proof if `create()` error routing ever becomes testable — Test 2 drives the constraint violation inside a test-local transaction rather than forcing `service.create()` itself to fail mid-transaction; the spec documents why direct failure-routing is structurally infeasible without production changes (accepted scope-of-proof NOTE from the MS3 closeout final review, 2026-06).
- **Watch entry.** Milestone 3: Keep the pages and navigation publication predicates in lockstep if standalone pages ever gain scheduled publishing (`publishedAt`-in-future) — divergence would create a pre-announcement leak (WA1 security NOTE, 2026-06). Current state (deferred-cleanup final review, 2026-06-07): the lockstep holds — deferred-cleanup subtask-6's public list endpoint (`GET /api/pages`) routes through `PagesService.findPublished()`, which applies the same `status = "published"` predicate as `findPublishedBySlug` and the nav filter; the watch remains only against a future scheduled-publishing change.
- **Fix on next pages-module touch.** Deferred-cleanup: Exercise the `StandalonePageEntity.currentRevision` relation — deferred-cleanup subtask-5 added the relation decorator (`createForeignKeyConstraints: false`; schema unchanged; boot-time metadata validity proven by the executed smoke validation), but no product code consumes it and no enabled test loads via `relations: ["currentRevision"]` (the DB-gated integration suite builds the entity metadata only). Add a DB-gated relation-loading assertion or a first product consumer (deferred-cleanup final review NOTE, 2026-06-07).
- Milestone 3 — decision recorded (no action): the `resolvePostId` → `createComment` TOCTOU window (`blog.controller.ts:276-280`) needs **no deferred remediation** — the service-layer non-public re-check (`blog.service.ts:388-397` after the deferred-cleanup edits) closes the race without oracle leakage. Recorded during the 2026-06-07 planning cycle so the MS3 closeout subtask-8 verifier's register citation has a real target; revisit only if the service-layer guard is removed. (Guard re-confirmed present through the deferred-cleanup changes, 2026-06-07.)
- **Design-doc/security hardening — no milestone assigned; suggested: a future security-hardening pass (design §13 / roadmap Phase 5).** Deferred-cleanup: Migrate the web CSP from `script-src`/`style-src 'unsafe-inline'` to nonce- or hash-based allowances — Next.js 15 hydration inline scripts/styles cannot be nonce-scoped without a custom server or middleware-injected nonces, so the deferred-cleanup subtask-2 baseline accepted `'unsafe-inline'` as a documented tradeoff; `apps/web/next.config.mjs` and `docs/features/web-shell.md` reference this entry as the registered candidate (deferred-cleanup subtask-2 tradeoff; recorded 2026-06-07).
- **Suggested: Milestone 6 (Downloads & Storage Pipeline; design §5.3) — batch with the upload-surface expansion.** Deferred-cleanup: Add magic-byte (content-sniffing) verification to media uploads — `apps/api/src/media/media.service.ts` validates the client-supplied content type against the image allow-list but never verifies the file bytes match the declared type, so a polyglot file with a whitelisted content-type header is stored as-is; keep SVG excluded from the allow-list when this lands (subtask-8 specialist security report finding M2, 2026-06-07).
- **Fix on next dev cycle (tester).** Deferred-cleanup: Deliver the two plan-mandated executed behavioral tests that remain open from the deferred-cleanup final review (its single residual WARNING): (a) an executed proxy-hop test proving `request.ip` resolves the original client IP from `X-Forwarded-For` under `trust proxy = 1` (subtask-1 AC2 demanded executed proof, not source inspection — current `index.test.ts` only asserts the mocked setter call); (b) API response-header emission assertions for the helmet baseline (`X-Content-Type-Options: nosniff` present, `Strict-Transport-Security` absent, no CSP header — current HSTS test is vacuous). Both likely require a supertest-class harness/dependency decision, which is why they were not patched ad hoc (deferred-cleanup final review residual WARNING, 2026-06-07).
- **Fix on next blog-module touch.** Deferred-cleanup: Map duplicate-key errors on explicit (caller-supplied) blog slugs to a controlled `409` envelope — `create()` with an explicit slug (`blog.service.ts:197-208`) and slug-changing `update()` (`:234-236`) still surface a raw duplicate-key error as an unhandled 500 on collision; the deferred-cleanup subtask-4 retry/409 hardening deliberately covered only the auto-derived path (deferred-cleanup final review NOTE, 2026-06-07).
