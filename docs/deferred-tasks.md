# Deferred Tasks

- **Milestone 1 (high priority — docs claim this exists):** Implement the locked trusted-proxy configuration for the API behind nginx-proxy — set Express `trust proxy` (or equivalent Nest adapter config) and document the forwarded-header contract. Auth audit logging records `request.ip` in ~9 call sites and currently captures the proxy's IP in production. `docs/architecture/milestone-1-foundation-decisions.md:117` and `docs/operations/deployment.md:94` describe this as already configured, but no code implements it (MS1 final-review WARNING; re-verified still open 2026-06-07).
- **Milestone 1 (high priority):** Implement the locked baseline security-header/CSP policy — no helmet/`Content-Security-Policy`/`X-Frame-Options`/`Referrer-Policy`/HSTS configuration exists in `apps/web/next.config.mjs`, the API bootstrap, or the deployment topology. Decide the enforcement point (Next `headers()`, API middleware, or proxy) and document required exceptions such as Swagger (`docs/architecture/milestone-1-foundation-decisions.md:118`; MS1 final-review WARNING; re-verified still open 2026-06-07).
- Milestone 2: Add additional external authentication providers beyond Google and GitHub, including Facebook and other common identity sources, using the same provider-linking foundation.
- Milestone 2: Add username changes after initial selection, including rate limiting, profile URL history or redirects, and audit-safe handling for mentions and identity continuity.
- Milestone 2: Add an executed runtime web test for register/login error mapping — every `apps/web` spec is source-contract style (no fetch mocking anywhere in the web suite); export or restructure `toApiRequestError`/`describeRegistrationError` (`apps/web/app/register/page.tsx:34,49`) so a test can drive mocked 400/409/5xx/network-failure responses and prove 5xx masking and duplicate-email messaging at runtime (auth-follow-up final-review follow-up; re-verified still open 2026-06-07).
- Milestone 3: Add arbitrary file attachments and broader upload/download support beyond the shared image-only baseline for blog posts, standalone pages, and blog comments.
- Milestone 3: Add standalone-page block-builder capabilities such as hero, image, and CTA blocks beyond the shared editor plus revision-history scope.
- Milestone 3: Add navigation trees deeper than one level of dropdown children if future site growth proves the extra hierarchy is necessary.
- Milestone 3: Add project-scoped authoring-policy controls such as owner-only, moderator-only, or member-authoring selection when the Projects milestone introduces project content ownership and membership management.
- Milestone 3: Keep the full wiki/documents feature set, including nested wiki hierarchy and broader document workflows, deferred to Milestone 5.
- Milestone 3: Add SEO surfaces — OpenGraph/meta tags, sitemaps, and canonical URLs for blog posts and standalone pages (design §12; roadmap Phase 5).
- Milestone 3: Add a reports / moderation queue for arbitrary objects beyond per-comment moderation (design §13; forums-era scope).
- Milestone 3: Add a per-user editor-mode preference (e.g. `user_settings.content_prefs`) plus a unified media-library picker with drag-and-drop, beyond the shared per-field image upload (design §10).
- Milestone 3: Complete a full WCAG 2.1 AA accessibility sweep beyond the keyboard-accessible navigation delivered in Milestone 3 (design §11.1; roadmap Phase 5).
- Milestone 3: Add a true WYSIWYG editing surface; Milestone 3 ships Markdown authoring with live preview only.
- Milestone 3: Add comment rate-limiting / anti-spam controls (design rate-limits to Phase 5 and forum anti-spam to Milestone 4; the current stack has no Redis).
- Milestone 3: Harden blog-post `deriveUniqueSlug` against the concurrent-create TOCTOU (catch the duplicate-key error on save and retry derivation with a suffix) so a losing concurrent create succeeds instead of surfacing a 500. Accepted characteristic per the MS3 final review (2026-06): the DB unique constraint already prevents any corruption and the surface is admin-only.
- Milestone 3: Trim the public blog-comment payload to what the UI needs — it currently exposes `authorUserId`, `moderatedByUserId`, and `moderatedAt` (data-minimization note from the retroactive MS3 security review, 2026-06).
- Milestone 3: Batch or cache the navigation publication-filter lookups if the nav tree grows — the public (and, post-closeout, authenticated) nav paths perform bounded per-item indexed point queries today (performance note from the retroactive MS3 security review, 2026-06).
- Milestone 3: Harden `validateUrl` for internal navigation items to require a leading `/` (defense-in-depth; admin-only input today — note from the retroactive MS3 security review, 2026-06).
- Milestone 3: Normalize the comment-creation second-order existence distinguishers — after the visibility gate, distinct 400 messages for `parentId` ('Parent comment not found.' vs 'Parent comment does not belong to this post.', `blog.service.ts:398-403`) and for `imageId` scope (`blog.service.ts:413-419`) let an authenticated member confirm foreign comment/media UUIDs exist; normalize the messages or fold into the comment-payload-trimming entry above to remove the last known authenticated existence signal on the comment path (MS3 closeout final review follow-up; subtask-8 security NOTE 4, 2026-06).
- Milestone 3: Pin the `LessThanOrEqual` FindOperator type in `navigation.service.test.ts`'s publication-predicate assertions to match the blog-side operator-type pinning (`blog.service.test.ts:204-238`), closing the shape-only residual where a regression to plain equality on the nav path would still pass the nav suite; batch with the next navigation or pages touch (MS3 closeout final review follow-up; subtask-7 security NOTE 1, 2026-06).
- Milestone 3: Perform a one-time check of deployed environments for a legacy `standalone_pages` row with slug `pages` (legal before the reserved-slug plan; plan Risk #4 assumed none exists) and delete or rename it if found, fully closing the prospective-only reserved-slug enforcement residual — `assertSlugValid` runs only on create and slug-changing update (MS3 closeout final review follow-up, 2026-06).
- Milestone 3: Resolve the bare-`/pages` dead-link residual — an admin-authored bare-`/pages` navigation item renders in the nav but resolves web-side to a deliberate not-found state because no `/pages` index route exists; adding a `/pages` index page would make it consistent automatically (fail-closed, admin-authored links only — accepted NOTE from the MS3 closeout final review, 2026-06).
- Milestone 3: Strengthen the pages-service integration rollback proof if `create()` error routing ever becomes testable — Test 2 drives the constraint violation inside a test-local transaction rather than forcing `service.create()` itself to fail mid-transaction; the spec documents why direct failure-routing is structurally infeasible without production changes (accepted scope-of-proof NOTE from the MS3 closeout final review, 2026-06).
- Milestone 3: Revisit the createComment Swagger-decorator source-contract assertions' 600-character lookback window in `blog.controller.test.ts:230` if the decorator block grows substantially (~300 chars today) — an expected decorator drifting out of the window makes the targeted assertions fail loudly rather than silently pass (informational NOTE from the MS3 closeout final review, 2026-06).
- Milestone 3: Run the retroactive specialist security review of the media upload/serve subsystem (`apps/api/src/media/`) — ms3-completion subtasks 2–6 were marked security-review-required but no specialist stage ran; later WA1/closeout reviews covered navigation and comments, leaving media (file upload handling, content-type validation, serve path) as the only marked surface never specialist-reviewed (ms3-completion final-review NOTE; recorded 2026-06-07).
- Milestone 3: Add BlogController controller-layer tests for the input parsers and admin gate wiring — `parseCreateInput` (`blog.controller.ts:419`), `parseUpdateInput` (`:441`), and `parsePublishAtInput` (`:475`) rejection paths and the per-handler `resolveSession` + `assertAdminManagementAccess` sequences have no coverage; `blog.controller.test.ts` covers only the resolvePostId fallback wiring and Swagger source contracts (MS3 subtask-3 verifier NOTE; re-verified still open 2026-06-07).
- Milestone 3: Validate `featuredMediaId` existence in `pages.service.ts` — create/update/restore persist it unchecked (`pages.service.ts:172,237,310`) while blog validates `featuredImageId` via `assertFeaturedImageExists` (`blog.service.ts:521-524`); align pages with the blog pattern (ms3-completion subtask-5 verifier NOTE; re-verified still open 2026-06-07).
- Milestone 3: Narrow the over-broad markdown-sanitizer rejection patterns — `/on\w+\s*=/i` and `/data\s*:/i` (`apps/api/src/media/markdown-sanitizer.ts:21,33`) reject legitimate prose like "training data: source A"; anchor them to link/attribute context and update the tests that currently codify the broad behavior (MS3 subtask-2 verifier NOTEs; re-verified still open 2026-06-07).
- Milestone 3: Add the missing relation decorator for `StandalonePageEntity.currentRevisionId` (`standalone-page.entity.ts:31` is a plain column; the FK exists in the migration) so the ORM can load the current revision via relations (MS3 subtask-1 verifier WARNING, accepted at the stub stage and never added).
- Milestone 3: Remove the dead private method `resolveCurrentBody` in `apps/api/src/pages/pages.controller.ts:218` (superseded by `resolveCurrentRevision`; no callers remain).
- Milestone 3: Move `const { memoryStorage } = multer;` (`apps/api/src/media/media.controller.ts:19`) below the import block (cosmetic; ms3-completion subtask-2 verifier NOTE).
- Milestone 3: Assert the `statusCode === null` register network-failure branch in `apps/web/app/public-shell.spec.ts` — the register condition is `statusCode === null || statusCode >= 500` (`register/page.tsx:65`) but only the 400/409/`>= 500` branches are pinned (ms3-landing subtask-1 verifier NOTE; re-verified still open 2026-06-07).
- Milestone 3: Keep the pages and navigation publication predicates in lockstep if standalone pages ever gain scheduled publishing (`publishedAt`-in-future) — the nav filter (`status = "published"`) exactly matches `findPublishedBySlug` today; divergence would create a pre-announcement leak (WA1 security NOTE, 2026-06).
- Milestone 3: Strengthen the reserved-slug parity tests from containment to set-equality — the web (`pages.spec.ts:295-314`) and API exhaustive-rejection tests assert all eleven entries are present but do not pin cardinality, so a twelfth entry added on one side only would not fail either contract (MS3 closeout security NOTE, 2026-06).
- Milestone 3: Add opportunistic navigation tests — a moderator-role caller of `findForAuthenticatedUser` (filtered today by the rank check, correct by inspection) and the all-children-filtered-out-but-parent-visible edge (MS3 closeout subtask-7 verifier/security NOTEs, 2026-06).
- Milestone 3 — decision recorded (no action): the `resolvePostId` → `createComment` TOCTOU window (`blog.controller.ts:276-280`) needs **no deferred remediation** — the service-layer non-public re-check (`blog.service.ts:382-386`) closes the race without oracle leakage. Recorded during the 2026-06-07 planning cycle so the MS3 closeout subtask-8 verifier's register citation has a real target; revisit only if the service-layer guard is removed.
- **Fix on next dev cycle:** Repair the stale runtime-contract assertion in `cicd/tests/run-validations.sh` that expects `DB_HOST=mysql` in `apps/api/.env.example` — the example legitimately ships the hybrid-dev default `DB_HOST=127.0.0.1` (the Compose files override to `mysql` for containers only), so `bash cicd/tests/run-validations.sh` currently fails on `main` at that check. Update the test to assert the hybrid default (and, if desired, the Compose-level `mysql` override) per `docs/operations/launch.md`. Found 2026-06-07 during the documentation restructure.
